# Aesthetic DESIGN.md

Public-site lock record for the Aesthetic marketing surface.

## Operator Lock

- Lock date: operator lock-in 2026-07-07, `misty-step-936`.
- Homepage H1, exact: `The Misty Step design system.`
- Layout: Split.
- Hero image: `site/assets/hero.jpg`, copied from the locked production asset
  `aesthetic-hero.jpg`.
- Image provenance: gpt-image-1, Misty Step fresco language.
- Image opacity: `0.85`.
- Homepage is hero-only: one viewport, no scroll, background image behind the
  full screen, copy block left-aligned, CTA uses `.ae-button`.

## Site Map

- `site/index.html`: locked hero.
- `site/primitives.html`: the original live 33-primitive gallery, including
  hash panel routing.
- `site/tokens.html`: generated by `scripts/build-tokens.mjs`; never hand-edit.
- `site/law.html`: law proof page.
- `site/changelog.html`: release notes.
- `site/get-started.html`: install/onboarding block sourced from the repo
  README and package version. This is where the pinned install command lives:
  `pnpm add github:misty-step/aesthetic#v2.24.0`.

## Chrome Contract

- Header: Lucide swatch-book mark in `.ae-app-mark`, uppercase `AESTHETIC`
  wordmark, nav `primitives · tokens · law · changelog · github`.
- Footer: mode toggle on the left. Right side reads
  `a Misty Step project`, with `Misty Step` linking to
  `https://mistystep.io`, followed by an inline SVG GitHub glyph linking to
  `https://github.com/misty-step/aesthetic`.
- No bare URL text, email, copyright line, or Weave footer links.

## Aesthetic Rules

- Aesthetic uses its own token vocabulary. Do not add a second palette, radius,
  type scale, shadow system, or custom CTA style.
- The gallery remains the product proof: every primitive is rendered live by
  the same stylesheet users install.
- Generated registry/feed surfaces now derive from `site/primitives.html`, not
  the locked homepage.
