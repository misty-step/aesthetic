# Retention verdict

The blind pass emitted 18 raw systems. The decision catalog retains 13 plus the shipped baseline: enough breadth to expose meaningfully different futures without presenting known duplicates as separate decisions.

Five are archived, not deleted:

- `MIN-3` overlaps `HALL-1` in indexed operational-ledger grammar.
- `SOFT-1` overlaps `MIN-1` and `IMP-1` in the editorial folio family.
- `ANTH-1` overlaps `MIN-2` in annotated field-note structure.
- `HALL-2` overlaps `MIN-1` in quiet editorial-ledger structure.
- `HALL-3` overlaps `ANTH-2` in relational signal-loom structure.

The retained set preserves two editorial systems, two soft/spatial systems, the full industrial trio, two relational/archival outliers, and four contemporary operational systems. This is an exploration verdict, not a production convergence decision.
