# LAB 002 evidence receipt

## Outcome

- Raw bench: 18 propositions from six blind philosophies, three each.
- Retained catalog: 15 candidates plus the shipped baseline.
- Effective spread: 12 distinct whole-system families after mutation and
  cross-lane dedupe.
- Final fresh-context design-director verdict: **ACCEPT**; no operator-facing
  blocker.

The strongest decision candidates were:

1. `ANTH-1` — focus aperture: inspect one live specimen without losing the
   complete gallery context.
2. `MIN-3` — indexed reading room: calm, scalable reference architecture.
3. `TASTE-2` — instrument ledger: dense, audit-oriented canonical proof.
4. `HALL-2` — continuous atlas: editorial family relationships without losing
   breadth.
5. `IMP-3` — state before source: behavioral proof precedes implementation
   markup.

## Browser oracle

`node explorations/lab-002/verify.mjs`

- 16 total options: 15 retained candidates plus baseline.
- 18 raw candidates recorded; six lanes × three proposals.
- 64 retained render cases: 390 and 1440, light and dark.
- Every case loaded the live canonical inventory: 34 cards and 35 routes.
- Every case had exactly one visible route, no horizontal overflow, no
  nonzero radius, no oversized type, and zero console errors.
- Desktop and phone screenshots were captured for every retained option in
  both modes under `evidence/retained/`.

The separate live QA walk opened the paged registry through
`http://127.0.0.1:8126/explorations/lab-002/`, observed all 15 candidates and
the baseline through accessible controls, and exercised the viewport, mode,
selection, and nested canonical-gallery path.

## Repo gate

`npm run check` passes. `npm run ci` reaches the existing Playwright suite but
does not pass on `master`: 34 tests still navigate the old gallery route at
`/site/#…`, while the locked homepage now owns `/site/` and the live catalog
is `/site/primitives.html#…`. This branch does not change `site/`, `tests/`, or
the Playwright configuration; the mismatch is pre-existing and is not hidden
or waived here.
